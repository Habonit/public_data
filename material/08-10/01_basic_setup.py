"""
예제 1: Streamlit 기본 설정 및 텍스트 표시

이 예제에서는 Streamlit 앱의 기본 설정과 다양한 텍스트 표시 방법을 학습합니다.
"""
import streamlit as st

# ============================================
# 페이지 설정 (반드시 가장 먼저 실행되어야 함!)
# ============================================
st.set_page_config(
    page_title="기본 설정 예제",  # 브라우저 탭에 표시되는 제목
    page_icon="📚",  # 브라우저 탭의 아이콘
    layout="wide",  # "wide" 또는 "centered"
    initial_sidebar_state="expanded"  # "expanded" 또는 "collapsed"
)

# ============================================
# 텍스트 표시 - Title
# ============================================
st.title("📚 Streamlit 기본 설정 및 텍스트 표시")

# ============================================
# 텍스트 표시 - Header와 Subheader
# ============================================
st.header("1. 헤더 (Header)")
st.write("헤더는 제목보다 한 단계 작은 크기의 제목입니다.")

st.subheader("1.1 서브헤더 (Subheader)")
st.write("서브헤더는 헤더보다 한 단계 작은 크기입니다.")

# ============================================
# 텍스트 표시 - Markdown
# ============================================
st.header("2. 마크다운 (Markdown)")

st.markdown("""
Streamlit에서는 마크다운 문법을 사용하여 풍부한 텍스트를 표시할 수 있습니다.

### 마크다운으로 할 수 있는 것들:

- **굵은 글씨**: `**텍스트**` 또는 `__텍스트__`
- *기울임*: `*텍스트*` 또는 `_텍스트_`
- ~~취소선~~: `~~텍스트~~`
- `코드`: \`코드\`

#### 목록:
1. 첫 번째 항목
2. 두 번째 항목
3. 세 번째 항목

#### 링크와 이미지:
- 링크: [Streamlit 공식 문서](https://docs.streamlit.io)
- 코드 블록:
```python
import streamlit as st
st.write("Hello, Streamlit!")
```
""")

# ============================================
# 텍스트 표시 - Write (만능 함수)
# ============================================
st.header("3. st.write() - 만능 표시 함수")

st.write("st.write()는 거의 모든 것을 표시할 수 있는 만능 함수입니다:")
st.write("- 텍스트")
st.write("- 숫자:", 42)
st.write("- 딕셔너리:", {"name": "Streamlit", "version": "1.28"})
st.write("- 리스트:", [1, 2, 3, 4, 5])

# ============================================
# 추가 텍스트 표시 방법들
# ============================================
st.header("4. 기타 텍스트 표시 방법")

# 일반 텍스트
st.text("st.text()는 고정폭 글꼴로 텍스트를 표시합니다.")
st.text("여러 줄도\n가능합니다.")

# 캡션
st.caption("st.caption()은 작은 회색 텍스트를 표시합니다. 설명이나 주석에 유용합니다.")

# 코드
st.code("""
# Python 코드 블록
def hello():
    print("Hello, World!")
""", language="python")

# 라텍스 (수식)
st.header("5. 수식 표시 (LaTeX)")
st.latex(r"""
a^2 + b^2 = c^2
""")

# ============================================
# 구분선
# ============================================
st.markdown("---")
st.caption("이것이 Streamlit의 기본 텍스트 표시 방법입니다!")

# ============================================
# 실습 섹션
# ============================================
st.header("🎯 실습해보세요!")
st.markdown("""
1. `page_title`과 `page_icon`을 변경해보세요
2. `layout`을 "centered"로 변경하고 차이를 확인해보세요
3. 자신만의 마크다운 텍스트를 작성해보세요
4. st.write()로 다양한 데이터 타입을 출력해보세요
5. 질문: st.write()와 st.text()의 차이는 무엇인가요?
""")

# --------------------------------------------
# 실습 1: 다양한 마크다운 스타일 실습
# --------------------------------------------
st.subheader("실습 1: 마크다운 스타일 실습")

st.markdown("""
> 💡 **마크다운 팁**: 인용구는 `>` 기호로 시작합니다.

| 함수 | 용도 | 특징 |
|------|------|------|
| `st.write()` | 범용 출력 | 자동 타입 감지 |
| `st.text()` | 고정폭 텍스트 | 코드 스타일 |
| `st.markdown()` | 마크다운 렌더링 | HTML 지원 |
| `st.caption()` | 작은 설명 | 회색 텍스트 |

---

**색상 있는 텍스트 (HTML 지원)**:
- :red[빨간색 텍스트] → `:red[텍스트]`
- :blue[파란색 텍스트] → `:blue[텍스트]`
- :green[초록색 텍스트] → `:green[텍스트]`
- :orange[주황색 텍스트] → `:orange[텍스트]`
""")

# --------------------------------------------
# 실습 2: st.write()와 st.text() 차이점
# --------------------------------------------
st.subheader("실습 2: st.write() vs st.text() 비교")

col1, col2 = st.columns(2)

with col1:
    st.write("**st.write() 출력:**")
    st.write("Hello World")
    st.write("딕셔너리:", {"key": "value", "number": 123})
    st.write("리스트:", [1, 2, 3])

with col2:
    st.write("**st.text() 출력:**")
    st.text("Hello World")
    st.text("딕셔너리: {'key': 'value', 'number': 123}")
    st.text("리스트: [1, 2, 3]")

st.info("""
**핵심 차이점:**
- `st.write()`: 데이터 타입을 자동 감지하여 적절하게 렌더링 (마크다운, DataFrame, 차트 등)
- `st.text()`: 항상 고정폭 폰트로 문자열 그대로 출력 (서식 없음)
""")

# --------------------------------------------
# 실습 3: 직접 작성해보기
# --------------------------------------------
st.subheader("실습 3: 직접 작성해보기")

user_text = st.text_area(
    "아래에 마크다운 텍스트를 입력하고 결과를 확인해보세요:",
    value="### 나의 첫 마크다운\n\n**굵은 글씨**와 *기울임*을 테스트합니다.\n\n- 항목 1\n- 항목 2",
    height=150
)

st.write("**미리보기:**")
st.markdown(user_text)

# --------------------------------------------
# 교육용 정보
# --------------------------------------------
with st.expander("📚 알아두면 좋은 정보"):
    st.markdown("""
### st.set_page_config() 상세 옵션

```python
st.set_page_config(
    page_title="앱 제목",           # 브라우저 탭 제목
    page_icon="🎯",                 # 파비콘 (이모지 또는 이미지 경로)
    layout="wide",                  # "centered" | "wide"
    initial_sidebar_state="auto",   # "auto" | "expanded" | "collapsed"
    menu_items={                    # 우측 상단 메뉴 커스터마이징
        'Get Help': 'https://www.example.com/help',
        'Report a bug': "https://www.example.com/bug",
        'About': "# 앱 설명"
    }
)
```

### 자주 사용하는 page_icon 이모지
| 카테고리 | 이모지 |
|---------|--------|
| 데이터 | 📊 📈 📉 🗂️ |
| 설정 | ⚙️ 🔧 🛠️ |
| 경고 | ⚠️ 🚨 ❗ |
| 완료 | ✅ ✔️ 💯 |
| 검색 | 🔍 🔎 |

### 성능 팁
- `st.set_page_config()`는 **반드시 스크립트 최상단**에 위치해야 합니다
- 다른 Streamlit 명령어보다 먼저 호출되어야 합니다
- 한 번만 호출할 수 있습니다 (중복 호출 시 에러)
    """)
